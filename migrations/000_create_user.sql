-- Database user creation is handled by the official MySQL Docker entrypoint
-- from MYSQL_DATABASE, MYSQL_USER, and MYSQL_PASSWORD in docker-compose.yml.
-- Keep this file as a no-op for migration order compatibility.
SELECT 1;

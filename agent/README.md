# awg-routing-agent

This directory is reserved for the host-level routing agent required by the routing subsystem.

The panel phase currently prepares schemas, UI, revisions and delivery jobs. The production agent still needs to be implemented as a separate Ubuntu host service before routing policies can affect traffic.
